{% extends 'base.html.twig' %}
{% block body %}
    {% if is_granted('ROLE_ADMIN') %}
    <h2 class="text-center">Add User</h2>
    
    <div class="col-12">
    {{form_start(form)}}
    {{form_widget(form)}}
    {{form_end(form)}}
    </div>
    {% else %}
        <h2 class="text-center">You are not allowed to create a new user.</h2>
    {% endif %}
{% endblock %}