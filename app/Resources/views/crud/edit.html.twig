{% extends 'base.html.twig' %}
{% block body %}
    <h2 class="text-center mt-5">Edit infomation for {{user.fullName}}</h2>
    
    <div class="col-12">
    {{form_start(form)}}
    {{form_widget(form)}}
    {{form_end(form)}}
    </div>
{% endblock %}